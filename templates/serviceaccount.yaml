{{- if .Values.externalSecrets }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "team6-helm.serviceAccountName" . }}
  labels:
    {{- include "team6-helm.labels" . | nindent 4 }}
{{- end }}